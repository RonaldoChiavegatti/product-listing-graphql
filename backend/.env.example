# Database
# Development (local)
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
# Production (example with Neon)
# DATABASE_URL="postgresql://user:password@ep-something-123456.region.aws.neon.tech/database"

# JWT (use uma string segura em produção)
JWT_SECRET=your_jwt_secret_here

# Server
PORT=3000
NODE_ENV=development

# Nota: Em produção na Vercel, PORT e NODE_ENV são configurados automaticamente 