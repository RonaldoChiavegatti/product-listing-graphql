services:
  - type: web
    name: testeoak-backend
    env: node
    rootDirectory: backend
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: postgresql://ronaldocorrea_user:wV7WrTgR7KxXStnXdB5duBf1oNgXkEOP@dpg-cva2shlumphs73adf7ug-a.oregon-postgres.render.com/ronaldocorrea
      - key: JWT_SECRET
        sync: false
    healthCheckPath: /health
    autoDeploy: true
    plan: starter
    region: oregon 